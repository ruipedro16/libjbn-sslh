// glob_p = P
u64[NLIMBS] glob_p = {
  0xfeda34ffc48af071
};

// glob_mp = R - P == 2^(NLIMBS*64) - P
u64[NLIMBS] glob_mp = {
  0x0125cb003b750f8f
};

// glob_pm2 = P - 2
u64[NLIMBS] glob_pm2 = {
  0xfeda34ffc48af06f
};

// (glob_u0 * P[0]) mod 2^64 == -1
u64 glob_u0 = 0x55fbaa04fded2f6f;

// glob_oneM = R mod P == 2^(NLIMBS*64) mod P
u64[NLIMBS] glob_oneM = {
  0x0125cb003b750f8f
};

// glob_rM = R^2 mod P == 2^(2*NLIMBS*64) mod P
u64[NLIMBS] glob_rM = {
  0x3acc0750f4089dbb
};

